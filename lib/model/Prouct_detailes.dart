class Product {
  String images;

  String name;

  double info;

  bool price;
  double rate;
  bool isFavorite;

  Product(this.images, this.name, this.info, this.price, this.rate,
      this.isFavorite);
}
